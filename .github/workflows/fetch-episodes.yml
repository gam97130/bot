name: Update Episodes JSON

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 */6 * * *"  # Exécuter toutes les 6 heures

jobs:
  update_episodes:  # ⚠️ Remplacé le "-" par "_" pour éviter une éventuelle erreur
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Run bot script
        run: node javascript/bot.js "https://anime-sama.fr/catalogue/overlord/saison1/vf/"

      - name: Commit et push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

          # Vérifier si episodes.json a changé
          if git diff --quiet episodes.json; then
            echo "Aucune modification détectée, fin du script."
            exit 0
          fi

          git add episodes.json
          git commit -m "🔄 Mise à jour automatique de episodes.json"
          git push "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git" HEAD:main
